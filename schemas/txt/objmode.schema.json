{
  "file": "objmode.txt",
  "description": "Object animation mode definitions - maps mode names to animation tokens (e.g. Neutral->NU, Operating->OP). Merged with objtype.txt at load time.",
  "category": "Levels",
  "hidden": true,
  "columns": {
    "name": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Animation mode name (e.g. Neutral, Operating, Opened)",
      "engineVerified": true
    },
    "token": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Animation token code (e.g. NU, OP, ON, S1-S5)",
      "engineVerified": true
    }
  }
}
