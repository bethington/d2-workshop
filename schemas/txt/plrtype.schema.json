{
  "file": "plrtype.txt",
  "description": "Player animation type definitions - maps player type names to animation tokens. Merged with plrmode.txt at load time.",
  "category": "Lookup Tables",
  "hidden": true,
  "columns": {
    "name": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Player type name (e.g. Amazon, Sorceress, Paladin)",
      "engineVerified": true
    },
    "token": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Animation token code (e.g. AM, SO, PA, NE, BA)",
      "engineVerified": true
    }
  }
}
