{
  "file": "montype.txt",
  "description": "Monster type hierarchy — damage resistances and immunities by type.",
  "columns": {
    "Name": {
      "type": "string",
      "required": true,
      "description": "Internal name identifier"
    },
    "Token": {
      "type": "string",
      "description": "Animation token identifier"
    },
    "DT_Dir": {
      "type": "integer",
      "min": 1,
      "max": 16,
      "description": "Number of directional frames for Death (DT) animation mode"
    },
    "NU_Dir": {
      "type": "integer",
      "min": 1,
      "max": 16,
      "description": "Number of directional frames for Neutral (NU) animation mode"
    },
    "WL_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Walk (WL) animation mode"
    },
    "GH_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Get Hit (GH) animation mode"
    },
    "A1_Dir": {
      "type": "integer",
      "min": 1,
      "max": 16,
      "description": "Number of directional frames for Attack 1 (A1) animation mode"
    },
    "A2_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Attack 2 (A2) animation mode"
    },
    "BL_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Block (BL) animation mode"
    },
    "SC_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Skill Cast (SC) animation mode"
    },
    "S1_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Special 1 (S1) animation mode"
    },
    "S2_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Special 2 (S2) animation mode"
    },
    "S3_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Special 3 (S3) animation mode"
    },
    "S4_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Special 4 (S4) animation mode"
    },
    "DD_Dir": {
      "type": "integer",
      "min": 1,
      "max": 16,
      "description": "Number of directional frames for Dead (DD) animation mode"
    },
    "KB_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Knockback (KB) animation mode"
    },
    "SQ_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Sequence (SQ) animation mode"
    },
    "RN_Dir": {
      "type": "integer",
      "min": 1,
      "max": 8,
      "description": "Number of directional frames for Run (RN) animation mode"
    }
  }
}
