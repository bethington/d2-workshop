{
  "file": "storepage.txt",
  "description": "Store page lookup table - maps store page codes to internal indices for shop UI tab assignment.",
  "category": "Lookup Tables",
  "hidden": true,
  "columns": {
    "code": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Store page code identifier (used by item types to specify which shop tab items appear in)",
      "engineVerified": true
    }
  }
}
