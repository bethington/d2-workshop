{
  "file": "gamble.txt",
  "description": "Gambling item pool — items available from gambling NPCs. Engine resolves each code to an item record and sorts by level.",
  "columns": {
    "code": {
      "type": "string",
      "ref": "armor.txt|weapons.txt|misc.txt",
      "description": "Item code resolved to item record via binary search (e.g. cap, skp, hlm)",
      "engineVerified": true
    }
  }
}