{
  "file": "itemstatcost.txt",
  "description": "Stat definitions — controls how stats are stored, transmitted, displayed, and affect item costs.",
  "columns": {
    "ID": {
      "type": "integer",
      "min": 0,
      "max": 176,
      "required": true,
      "unique": true,
      "description": "Unique numeric ID"
    },
    "Add": {
      "type": "integer",
      "min": 0,
      "max": 1000,
      "description": "Flat cost modification value"
    },
    "Multiply": {
      "type": "integer",
      "min": 0,
      "max": 300,
      "description": "Multiplicative cost modifier"
    },
    "Divide": {
      "type": "integer",
      "min": 0,
      "max": 100,
      "description": "Division factor for calculations"
    },
    "CodeName": {
      "type": "string",
      "description": "Internal code name used to reference this stat in other data files"
    },
    "English like name": {
      "type": "string",
      "description": "Human-readable English description of the stat for documentation"
    },
    "WeirdCode": {
      "type": "string",
      "description": "Alternate or legacy internal code reference"
    },
    "ItemSpecific": {
      "type": "boolean",
      "description": "Whether this stat is stored per-item rather than per-character"
    }
  }
}
