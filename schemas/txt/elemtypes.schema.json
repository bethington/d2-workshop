{
  "file": "elemtypes.txt",
  "description": "Elemental damage type code lookup table. Engine uses row index to map element type IDs to their short codes.",
  "columns": {
    "code": {
      "type": "string",
      "description": "Short element type code used in formulas and data references (e.g. fire, cold, ltng, pois)",
      "engineVerified": true
    }
  }
}