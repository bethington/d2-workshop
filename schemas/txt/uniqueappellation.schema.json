{
  "file": "uniqueappellation.txt",
  "description": "Unique monster appellation (title suffix) name strings for random boss name generation.",
  "columns": {
    "Name": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Monster name component string table key",
      "engineVerified": true
    }
  }
}