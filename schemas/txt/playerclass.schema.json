{
  "file": "playerclass.txt",
  "description": "Player character class code lookup table. Engine uses row index to map class IDs to their short codes.",
  "columns": {
    "code": {
      "type": "string",
      "required": true,
      "unique": true,
      "description": "Three-letter player class code (e.g. ama, sor, nec, pal, bar, dru, ass)",
      "engineVerified": true
    }
  }
}